<!DOCTYPE html>
<!--[if lt IE 7]><html class="no-js lt-ie9 lt-ie8 lt-ie7"><![endif]-->
<!--[if IE 7]><html class="no-js lt-ie9 lt-ie8"><![endif]-->
<!--[if IE 8]><html class="no-js lt-ie9"><![endif]-->
<!--[if gt IE 8]><!--><html class="no-js"><!--<![endif]-->
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<title>
		AdminFlare - Sign In
	</title>
	<meta name="description" content="">
	<meta name="viewport" content="width=device-width">

	<script src="/assets/adminflare-demo-init.min.js" type="text/javascript"></script>

	<link href="http://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,400,300,600,700" rel="stylesheet" type="text/css">
	<link href="/assets/bootstrap.min.css" media="all" rel="stylesheet" type="text/css" id="bootstrap-css">
	<link href="/assets/css//adminflare.min.css" media="all" rel="stylesheet" type="text/css" id="adminflare-css">
	</script>
	<%= javascript_include_tag "application" %>
		<script src="<%= asset_path "modernizr-jquery.min.js"%>" type="text/javascript"></script>
	<script src="<%= asset_path "adminflare-demo.min.js"%>" type="text/javascript"></script>
	<script src="<%= asset_path "bootstrap.min.js"%>" type="text/javascript"></script>
	<script src="<%= asset_path "adminflare_min.js"%>" type="text/javascript"></script>
<%= stylesheet_link_tag "application", :media => "all" %>

	<style type="text/css">
		body {
			padding: 0;
			background-image: url(<%=asset_path "left-menu-bg.png"%>);
		}

		label a, .btn, input, label {
			font-size: 14px !important;
		}

		form {
			margin: 0 auto;
			padding: 0;
			position: relative;
			width: 250px;
		}

		input[type=text],
		input[type=password] {
			border: none;
			height: 32px;
			line-height: 32px;
			margin-bottom: 15px;
			opacity: 0.9;
			filter: ~"alpha(opacity=90)";
			padding: 0 10px;
			width: 100%;

			box-sizing:border-box;
			-moz-box-sizing:border-box; /* Firefox */
			-webkit-box-sizing:border-box; /* Safari */

			-webkit-box-shadow: none;
			-moz-box-shadow: none;
			box-shadow: none;
		}

		input[type=text]:focus,
		input[type=password]:focus {
			opacity: 1;
			filter: ~"alpha(opacity=100)";
		}

		label, label a:hover {
			color: #bbb;
		}

		label a {
			font-size: 12px !important;
			color: #666;
		}

		.btn {
			margin: 20px 0 0 0;
			line-height: 26px;
			border: none;
		}

		a.header {
			display: block;
			margin-bottom: 30px;
			font-size: 16px;
			line-height: 25px;
			padding-left: 75px;
			color: #fff;
		}

		a.header:hover {
			text-decoration: none;
		}

		a.header strong {
			font-size: 22px;
		}

		a.header img {
			display: block;
			position: absolute;
			margin: -6px 0 0 -50px;
		}

		a.header img, a.header span {
			opacity: 0.2;
			filter: ~"alpha(opacity=20)";
		}

		a.header:hover img, a.header:hover span {
			opacity: 0.6;
			filter: ~"alpha(opacity=60)";
		}

		/* Animations */
		form,
		input[type=text],
		input[type=password],
		a.header img,
		a.header span {
			-webkit-transition: all 0.2s;
			-moz-transition: all 0.2s;
			-o-transition: all 0.2s;
			transition: all 0.2s;
		}
	</style>
	
	<script type="text/javascript">
		$(document).ready(function() {
			$('form').submit(function() {
				document.location = 'dashboard.html';
				return false;
			});

			var updateBoxPosition = function() {
				$('form').css({
					'margin-top': ($(document).height() - $('form').height()) / 2
				});
			};
			$(window).resize(updateBoxPosition);
			updateBoxPosition();
		});
	</script>
</head>
<body>
	
	<!-- Page content
		================================================== -->
	<section class="signin_container">
		<%= yield %>
		
	</section>
</body>
</html>